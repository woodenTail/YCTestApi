# 安装第三方库

``` python
pip install jsonpath   
pip install pandas   
pip install Crypto 

# 一直报找不到Crypto模块，就找到python 目录下面的\Lib\site-packages，手动将crypto改为Crypto
# pip install pycryptodome（不成功继续执行这个）
pip install pymysql
pip install openpyxl
pip install pytest-assume

```
